services:
  # Next.js Frontend
  - type: web
    name: imdb-clone-frontend
    env: node
    plan: starter
    rootDir: cghilderen/imdb  # Specify the subfolder path
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        fromService:
          name: imdb-clone-backend
          type: web
          property: host
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
    autoDeploy: true

  # Express.js Backend
  - type: web
    name: imdb-clone-backend
    env: node
    plan: starter
    rootDir: imdb-be  # Assuming you'll create a backend folder at this path
    buildCommand: npm ci
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: FRONTEND_URL
        fromService:
          name: imdb-clone-frontend
          type: web
          property: host
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
    autoDeploy: true